const {expect} = require('chai');

const Multiply = require('./../Multiply'); 
describe('Multiply', function () {
 it('should return number of charachters in a string', function () {
    expect('hello').to.be.equal('pop');
  });
});